global:
  scrape_interval: 15s

scrape_configs:
  # OTel Collector 暴露的应用指标 (8889 端口)
  - job_name: otel-app
    static_configs:
      - targets: ["otel-collector:8889"]
  # Collector 自身
  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:8888"]
